.side-bar
  .side-bar__user-name
    .top-items
      masa
      .top-items__icons
        = link_to new_forum_path do
          %i.fa.fa-pencil-square-o.header-items__icons--pen
        = link_to edit_user_path(current_user.id) do
          %i.fa.fa-cog.header-items__icons--cog
  .side-bar__forums-list
    - current_user.forums.each do |forum|
      .forum
        = link_to '#' do
          .forum__name
            = forum.name
          .forum__message
            メッセージはまだありません。